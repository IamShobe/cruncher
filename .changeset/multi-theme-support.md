---
"cruncher": minor
---

Add multi-theme support (Midnight, Nord, Dracula, Catppuccin Mocha) with theme picker in Settings → General. Theme preference is persisted via localStorage and applied via CSS variable overrides scoped to the shadow DOM.
