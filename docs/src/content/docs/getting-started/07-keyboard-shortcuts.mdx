---
title: Keyboard Shortcuts
description: Complete keyboard shortcuts reference for Cruncher.
---

import KeyboardShortcuts from '../../../components/KeyboardShortcuts.astro';

<KeyboardShortcuts groups={[
  {
    label: "Global",
    shortcuts: [
      { action: "Shortcut reference", mac: "⌘ + /", windows: "Ctrl + /" },
      { action: "New tab",            mac: "⌘ + T", windows: "Ctrl + T" },
      { action: "Close tab",          mac: "⌘ + W", windows: "Ctrl + W" },
    ]
  },
  {
    label: "Search",
    shortcuts: [
      { action: "Full search",            mac: "⌘ + ⇧ + Enter", windows: "Ctrl + Shift + Enter" },
      { action: "Re-evaluate pipeline",   mac: "⇧ + Enter",     windows: "Shift + Enter" },
      { action: "Trigger autocomplete",   mac: "⌃ + Space",     windows: "Ctrl + Space" },
      { action: "Focus query editor",     mac: "⌥ + Q",         windows: "Alt + Q" },
      { action: "Open time range picker", mac: "⌥ + T",         windows: "Alt + T" },
    ]
  },
  {
    label: "UI",
    shortcuts: [
      { action: "Toggle live mode",   mac: "⌘ + L",     windows: "Ctrl + L" },
      { action: "Toggle until now",   mac: "⌘ + N",     windows: "Ctrl + N" },
      { action: "Toggle header",      mac: "⌘ + H",     windows: "Ctrl + H" },
      { action: "Focus highlighter",  mac: "⌘ + F",     windows: "Ctrl + F" },
      { action: "Copy share link",    mac: "⌘ + ⇧ + C", windows: "Ctrl + Shift + C" },
      { action: "Toggle idle hints",  mac: "⌘ + ⇧ + I", windows: "Ctrl + Shift + I" },
    ]
  }
]} />

**Re-evaluate vs Full Search** — `⇧ Enter` re-runs your pipeline without changing the time range. `⌘ ⇧ Enter` performs a full search and may reset the time window.

## Customizing keybindings

All shortcuts can be overridden in **Settings → Keybindings**. Click any key badge to record a new combo, then choose whether to save it to your **Local** config (`~/.config/cruncher/cruncher.config.local.yaml`) or the shared **Global** config (`~/.config/cruncher/cruncher.config.yaml`). Local overrides always take precedence over global ones. Click the **×** next to a binding to reset it to the default, or use **Reset All** to clear every override at once.
