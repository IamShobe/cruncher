---
title: Data Types
description: Learn about the different data types in Cruncher Query Language (QQL).
---

import DataTypeList2 from '/src/components/DataTypeList2.astro';

Data adapters assign types to extracted fields. Understanding these types helps you write correct queries and transformations.

<DataTypeList2 />

## Type Conversion in Queries

When you evaluate expressions, result types depend on the operands:

```qql
eval duration_sec = duration / 1000
```

If `duration` is a number, `duration_sec` is also a number. Missing fields become `null`.
