---
title: isNull
description: Check if a value is null or undefined.
---

import FunctionCard from '../../../../../components/FunctionCard.astro';
import QQLCode from '../../../../../components/QQLCode.astro';

<FunctionCard
  name="isNull"
  category="boolean"
  signature="isNull(value) → boolean"
  returnType="boolean"
  returnDescription="true if value is null or field is missing, false otherwise"
  description="Check if a value is null or a field is missing"
/>

The `isNull` function returns `true` if the input value is `null` or undefined (field not present), otherwise `false`.

## Edge Cases

- Distinguishes between `null` (explicit null value) and missing/undefined fields (both return `true`)
- Empty strings `""` are **not** null (returns `false`)
- Zero `0` and `false` are **not** null (returns `false`)
- `null` in a comparison like `field == null` is equivalent to `isNull(field)`

## Examples

<QQLCode code={`| where isNull(optional_field)`} />

Matches records where `optional_field` is missing or null.

<QQLCode code={`| eval has_user = !(isNull(user_id))`} />

Creates a boolean field `has_user` that is `true` if `user_id` is present.

<QQLCode code={`| where isNull(error_code)`} />

Filters to successful records (no error code).

## Used In

- [`where`](/qql-reference/commands/where) — as a filter condition
- [`eval`](/qql-reference/commands/eval) — to create a boolean field
