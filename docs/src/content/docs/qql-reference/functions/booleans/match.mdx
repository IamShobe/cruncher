---
title: match
description: Check if a string matches a regular expression.
---
import { Aside } from '@astrojs/starlight/components';

The `match` function returns true if the first argument matches the given regular expression.

## Usage

```
match(field, /regex/)
```

**Example:**
```
where match(message, `^Error:.*`)
```
This matches records where the `message` field matches the regex pattern `^Error:.*`.

<Aside type="caution">
Note that the regex must be enclosed in backticks (`` ` ``) to be interpreted correctly in QQL.
</Aside>