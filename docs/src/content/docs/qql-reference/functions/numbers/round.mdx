---
title: round
description: Round a number to the nearest integer.
---

import FunctionCard from '../../../../../components/FunctionCard.astro';
import QQLCode from '../../../../../components/QQLCode.astro';

<FunctionCard
  name="round"
  category="number"
  signature="round(number) → number"
  returnType="number"
  returnDescription="The input rounded to the nearest integer; null if input is null"
  description="Round to the nearest integer (half rounds up)"
/>

The `round` function rounds the input number to the **nearest integer**.

## Edge Cases

- If `number` is `null`, returns `null`
- `round(3.0)` returns `3` (already an integer)
- `round(3.5)` returns `4` (rounds half up)
- `round(3.4)` returns `3`
- `round(-2.5)` returns `-2` (rounds half up toward positive infinity)
- `round(0)` returns `0`

## Examples

<QQLCode code={`| eval rounded_duration = round(duration_ms / 1000)`} />

Rounds duration in milliseconds to the nearest second.

<QQLCode code={`| eval percentile_rounded = round(percentile)`} />

Rounds a percentile value to the nearest integer.

<QQLCode code={`| stats avg(round(latency_ms))`} />

Calculates average latency with each value rounded first.

## Used In

- [`eval`](/qql-reference/commands/eval) — to create or transform numeric fields
