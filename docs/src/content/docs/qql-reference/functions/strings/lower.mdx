---
title: lower
description: Convert a string to lowercase.
---

import FunctionCard from '../../../../../components/FunctionCard.astro';
import QQLCode from '../../../../../components/QQLCode.astro';

<FunctionCard
  name="lower"
  category="string"
  signature="lower(string) → string"
  returnType="string"
  returnDescription="The input string with all characters converted to lowercase; null if input is null"
  description="Convert all alphabetic characters to lowercase"
/>

The `lower` function converts the input string to lowercase.

## Edge Cases

- If `string` is `null`, returns `null`
- Non-alphabetic characters (numbers, punctuation, spaces) are unchanged
- Works with Unicode characters where case conversion is defined

## Examples

<QQLCode code={`| eval user_lower = lower(user)`} />

Creates a new field `user_lower` with the lowercase version of `user`, useful for case-insensitive comparisons.

<QQLCode code={`| where lower(status) == "error"`} />

Matches records where status is "error", "ERROR", "Error", etc.

<QQLCode code={`| eval normalized_service = lower(service_name)`} />

Normalizes service names to lowercase for grouping.

## Used In

- [`eval`](/qql-reference/commands/eval) — to create or transform string fields
