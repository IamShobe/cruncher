---
title: upper
description: Convert a string to uppercase.
---

import FunctionCard from '../../../../../components/FunctionCard.astro';
import QQLCode from '../../../../../components/QQLCode.astro';

<FunctionCard
  name="upper"
  category="string"
  signature="upper(string) → string"
  returnType="string"
  returnDescription="The input string with all characters converted to uppercase; null if input is null"
  description="Convert all alphabetic characters to uppercase"
/>

The `upper` function converts the input string to uppercase.

## Edge Cases

- If `string` is `null`, returns `null`
- Non-alphabetic characters (numbers, punctuation, spaces) are unchanged
- Works with Unicode characters where case conversion is defined

## Examples

<QQLCode code={`| eval user_upper = upper(user)`} />

Creates a new field `user_upper` with the uppercase version of `user`.

<QQLCode code={`| where upper(env) == "PRODUCTION"`} />

Matches records where env is "production", "Production", "PRODUCTION", etc.

<QQLCode code={`| eval level_upper = upper(level)`} />

Normalizes log level field to uppercase.
