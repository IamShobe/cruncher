---
title: sort
description: Sorts records by one or more fields in ascending or descending order.
---

import CommandCard from '/src/components/CommandCard.astro';
import QQLCode from '/src/components/QQLCode.astro';

<CommandCard
  name="sort"
  description="Order records by one or more fields in ascending or descending order."
  syntax="| sort FIELD_NAME [asc|desc], ..."
  example="| sort timestamp desc, user asc"
  output="logs"
/>

The `sort` command orders records by one or more fields, in ascending or descending order.

## Parameters

- `FIELD_NAME`: The fields to sort by.
- `asc`: Sort in ascending order (default).
- `desc`: Sort in descending order.

## Usage

- Use `sort` to control the order of results in the output table or view.
- Multiple fields can be specified for multi-level sorting.

## Example

<QQLCode code={`| sort timestamp desc, user asc`} />

This sorts results by `timestamp` descending, then by `user` ascending.
