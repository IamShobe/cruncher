---
title: QQL (Quick Query Language)
description: The main query language inside Cruncher for searching, filtering, and transforming log data.
---

import QueryPipeline from '../../../components/QueryPipeline.astro';
import QQLCode from '../../../components/QQLCode.astro';
import CommandRefGrid from '../../../components/CommandRefGrid.astro';

**QQL is the unified query language in Cruncher for searching, filtering, and transforming logs from any data source.**

Inspired by observability languages like Splunk SPL2 and Kusto KQL, QQL is designed to be easy to learn while providing powerful capabilities for log analysis and investigation.

## Why QQL?

Different log sources (Grafana, Loki, Kubernetes, Docker) have different native query languages. QQL abstracts away these differences, letting you write once and query any source. You get:

- **Consistent syntax** across all adapters
- **Powerful pipeline model** for data transformation
- **Simple learning curve** with familiar operators
- **Local execution** for fast feedback

## The Pipeline Model

QQL uses a **pipeline model**: data flows through a series of commands, with each command filtering, transforming, or visualizing the data.

<QueryPipeline
  title="How Data Flows Through QQL"
  steps={[
    {
      label: "Data Source",
      description: "Logs retrieved from your configured adapter",
      icon: "📥"
    },
    {
      group: true,
      label: "Pipeline Commands",
      stages: [
        { label: "Filter", description: "where", icon: "🔽" },
        { label: "Transform", description: "eval, regex", icon: "🔄" },
        { label: "Aggregate", description: "stats, timechart", icon: "📊" }
      ]
    },
    {
      label: "Output",
      description: "View results as Logs, Table, or Chart",
      icon: "📈"
    }
  ]}
/>

## Query Structure

A QQL query has two parts:

1. **Controller parameters** (optional): Passed to the adapter for server-side filtering
2. **Pipeline commands**: Executed locally on results

```
[controller_params] | command1 | command2 | command3
```

**Example:**

```qql
level=error | where contains(message, "timeout") | stats count by service
```

- No controller params in this example
- `where contains(...)` — filters records where message contains "timeout"
- `stats count by service` — counts records grouped by service field

## Commands Quick Reference

<CommandRefGrid commands={[
  { name: "where",      purpose: "Filter records by condition",       example: "where status == \"error\"",         link: "/qql-reference/commands/where" },
  { name: "eval",       purpose: "Create or modify fields",           example: "eval duration_ms = duration / 1000", link: "/qql-reference/commands/eval" },
  { name: "regex",      purpose: "Extract fields using a regex",      example: "regex \"(?P<code>\\d+)\" message",  link: "/qql-reference/commands/regex" },
  { name: "table",      purpose: "Select columns and display as table", example: "table timestamp, message, level", link: "/qql-reference/commands/table" },
  { name: "stats",      purpose: "Aggregate with count, sum, avg…",   example: "stats count() by level",            link: "/qql-reference/commands/stats" },
  { name: "timechart",  purpose: "Time-series aggregation",           example: "timechart count by level",          link: "/qql-reference/commands/timechart" },
  { name: "sort",       purpose: "Sort results by a field",           example: "sort timestamp desc",               link: "/qql-reference/commands/sort" },
  { name: "unpack",     purpose: "Flatten JSON / nested fields",      example: "unpack json",                       link: "/qql-reference/commands/unpack" },
]} />

See [Commands](/qql-reference/commands) for full details.

## Functions

QQL provides functions for boolean logic, string manipulation, math, and more:

**Boolean**
<CommandRefGrid commands={[
  { name: "contains()", purpose: "Check if a string contains a substring", example: 'contains(message, "error")', link: "/qql-reference/functions/booleans/contains" },
  { name: "startsWith()", purpose: "Check if a string starts with a prefix", example: 'startsWith(path, "/api")', link: "/qql-reference/functions/booleans/startsWith" },
  { name: "endsWith()", purpose: "Check if a string ends with a suffix", example: 'endsWith(file, ".log")', link: "/qql-reference/functions/booleans/endsWith" },
  { name: "match()", purpose: "Check if a string matches a regex pattern", example: "match(message, \"^Error:\")", link: "/qql-reference/functions/booleans/match" },
  { name: "isNull()", purpose: "Check if a value is null or missing", example: "isNull(optional_field)", link: "/qql-reference/functions/booleans/isNull" },
  { name: "isNotNull()", purpose: "Check if a value is not null", example: "isNotNull(user)", link: "/qql-reference/functions/booleans/isNotNull" },
]} />

**String**
<CommandRefGrid commands={[
  { name: "lower()", purpose: "Convert a string to lowercase", example: "lower(user)", link: "/qql-reference/functions/strings/lower" },
  { name: "upper()", purpose: "Convert a string to uppercase", example: "upper(level)", link: "/qql-reference/functions/strings/upper" },
  { name: "length()", purpose: "Return the character count of a string", example: "length(message)", link: "/qql-reference/functions/strings/length" },
  { name: "trim()", purpose: "Remove leading and trailing whitespace", example: "trim(raw_field)", link: "/qql-reference/functions/strings/trim" },
]} />

**Number**
<CommandRefGrid commands={[
  { name: "abs()", purpose: "Return the absolute value of a number", example: "abs(delta)", link: "/qql-reference/functions/numbers/abs" },
  { name: "round()", purpose: "Round a number to the nearest integer", example: "round(duration_ms)", link: "/qql-reference/functions/numbers/round" },
  { name: "ceil()", purpose: "Round a number up to the nearest integer", example: "ceil(ratio)", link: "/qql-reference/functions/numbers/ceil" },
  { name: "floor()", purpose: "Round a number down to the nearest integer", example: "floor(ratio)", link: "/qql-reference/functions/numbers/floor" },
]} />

**Conditional**
<CommandRefGrid commands={[
  { name: "if()", purpose: "Return one of two values based on a condition", example: 'if(status == "error", "critical", "ok")', link: "/qql-reference/functions/conditional/if" },
  { name: "case()", purpose: "Return a value from multiple condition branches", example: 'case(status == "error", "A", "B")', link: "/qql-reference/functions/conditional/case" },
]} />

See [Functions](/qql-reference/functions) for full details.

## Data Types

QQL supports: `string`, `number`, `boolean`, and `null`. The adapter assigns types when extracting fields, and type coercion happens implicitly in expressions.

See [Data Types](/qql-reference/03-data-types) for details.

## Complete Example

Suppose you want to investigate slow API requests in your logs:

<QQLCode code={`service="api" method="POST"
  | eval duration_ms = duration / 1000
  | where duration_ms > 100
  | stats avg(duration_ms), max(duration_ms), count() by endpoint
  | sort count desc`} />

**What happens:**

1. Adapter filters for `service=api` and `method=POST` before sending data
2. Pipeline creates `duration_ms` field in milliseconds
3. Filters to requests slower than 100ms
4. Aggregates by endpoint: avg/max duration, count
5. Sorts by count descending

**Result:** A table showing which endpoints have the most slow requests.

---

**Next steps:**
- [Query Structure](/qql-reference/02-query) — Controller params and syntax details
- [Commands](/qql-reference/commands) — Full command reference
- [Functions](/qql-reference/functions) — All available functions

